# ~/.bashrc.d/10_wsl

if [[ -f /proc/version && $(</proc/version) == *Microsoft@Microsoft* ]]; then
	# Set correct umask
	# Microsoft/BashOnWindows#352
	if [[ "$(umask)" == '0000' ]]; then
		umask 022
	fi
fi

